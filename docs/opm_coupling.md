# OPM coupling notes

The package can generate OPM/ECL-style VFPINJ tables and can be used externally to map:

```text
THP + rate -> BHP/BHT
```

or inversely:

```text
target BHP + rate -> required THP
```

Check exact VFPINJ syntax compatibility with your OPM Flow version before production use.
